{
  "name": "@gamevault/tetris",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "esbuild src/main.ts --bundle --outfile=dist/bundle.js --sourcemap --watch --serve=3005 --servedir=dist",
    "build": "esbuild src/main.ts --bundle --outfile=dist/bundle.js --minify --sourcemap && node scripts/copy.mjs"
  },
  "dependencies": {
    "@gamevault/game-sdk": "workspace:*",
    "three": "^0.172.0"
  },
  "devDependencies": {
    "@types/three": "^0.172.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0"
  }
}
