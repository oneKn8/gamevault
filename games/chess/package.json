{
  "name": "@gamevault/chess",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "esbuild src/main.ts --bundle --outfile=dist/bundle.js --sourcemap --watch --serve=3004 --servedir=dist",
    "build": "esbuild src/main.ts --bundle --outfile=dist/bundle.js --minify --sourcemap && node scripts/copy.mjs"
  },
  "dependencies": {
    "chess.js": "^1.0.0-beta.8",
    "@gamevault/game-sdk": "workspace:*",
    "@gamevault/neon-theme": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0"
  }
}
